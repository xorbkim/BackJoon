List 순서o 중복o
ArrayList: 배열사용

ArrayList() 기본 생성자
ArrayList(Collection c)
ArrayList(int initialCapacity) 베열의 길이

add() 추가 맨뒤에
add(int index, Object element) 추가, 위치 지정
addAll(Collection c) 전부 추가
addAll(int index, Collection c) 전부 추가, 위치 지정

remove(Object o) 삭제
remove(int index) 위치 삭제
removeAll(Collecion c)
clear() 모든 책체 삭제

indexOf(Object o) 객체가 몇번째 있는지 검색 (없으면 -1)
lastIndexOf(Object o) 맨 뒤부터 탐색
contains(Object o) 객체를 포함하는가 (boolean)
get(int index) 객체를 읽기
set(int index, Object element) 특정 위치에 있는 것을 변경

subList(int fromIndex, int toIndex) from~to로 새로운 List 생성
toArray() ArrayList의 객체배열 반환
isEmpty() 비어있는지 (boolean)
trimToSize() 빈공간 제거
size() 저장된 객체의 개수